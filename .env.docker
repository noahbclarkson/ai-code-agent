# Environment Variables

# Single API key (use either this OR GEMINI_API_KEYS below)
GEMINI_API_KEY=your_api_key_here

# Multiple API keys for rotation (comma-separated)
# Example: key1,key2,key3
# GEMINI_API_KEYS=key1,key2,key3

# Gemini model to use
GEMINI_MODEL=gemini-2.5-pro

# Token character limit (default: 200000)
TOKEN_CHAR_LIMIT=200000

# Path to codebase_viewer executable
# The AI will provide full paths to analyze in tool calls
# Use forward slashes for Windows paths in .env files
CODEBASE_VIEWER_PATH=C:/Users/xyz/Documents/GitHub/codebase_viewer/target/release/codebase_viewer.exe
